
let UI = document.createElement("span");
/** @type {string} */
UI.innerHTML = '\n        <span id="SLbQ" style="width:300px; left: 1px; top: 1px; background-color: #282828; color: white; outline: white solid 1px; position:absolute; z-index: 99999;">\n            <h1 style="font-size: 32px;">' + genTitle() + "</h1>\n    \n            <br>\n    \n            <i>" + genInfo() + '</i>\n    \n            <br>\n            <br>\n    \n            <h2 style="font-size: 25px; font-style: normal !important; color: white !important;">' + genSkipper() + '</h2>\n    \n            <button onclick="skipLesson(this)">' + 
genSkip() + '</button>\n    \n            <br>\n            <br>\n    \n            <h2 style="font-size: 25px; font-style: normal !important; color: white !important;">' + genMinutes() + '</h2>\n    \n            <button onclick="farmMinutes(this)">' + genOtherMinutes() + '</button>\n    \n        <br>\n            <br>\n    \n            <h2 style="font-size: 25px; font-style: normal !important; color: white !important;">' + genDiagnostic() + '</h2>\n    \n            <button onclick="diagnosticHack(this)">En\u200e\u200ba\u2064bl\u200be</button>\n                    <br>\n            <br>\n            <hr>\n            to\u200e\u200b\u200bo\u200bl b\u200b\u200ey <a style="color: #3d69c4; text-decoration: underline; cursor: pointer;" id="gits">' + 
genName() + '</a> | <a style="color: #3d69c4; text-decoration: underline; cursor: pointer;" id="gits2">' + genName2() + "</a>\n            <br>\n            <br>\n        </span>";
let functionsScript = document.createElement("script");
functionsScript.innerHTML = "let skippingLesson = false; let skippingLesson2 = false; let minuteFarming = false;  \n " + skipLesson.toString() + " \n " + farmMinutes.toString() + " \n " + diagnosticHack.toString() + " \n " + getCookie.toString() + ' document.addEventListener(\'keydown\', function(e){ if (event.ctrlKey && event.key == "z"){ if(SLbQ.style.display == "block"){ SLbQ.style.display = "none"; } else { SLbQ.style.display = "block"; } } });\n', document.body.appendChild(functionsScript), 
dragElement(UI.firstElementChild), document.body.appendChild(UI), document.getElementById("gits").addEventListener("click", function() {
  const viewSourceElement = {};
  /** @type {string} */
  viewSourceElement.PNnlq = "https://github.com/cupiditys/iReady-Overload";
  /** @type {string} */
  viewSourceElement.WLaKg = "_blank";
  const poweredBy = viewSourceElement;
  window.open(poweredBy.PNnlq, poweredBy.WLaKg).focus();
}), document.getElementById("gits2").addEventListener("click", function() {
  const viewSourceElement = {};
  /** @type {string} */
  viewSourceElement.JfitT = "https://discord.gg/y7X5CSWyR5";
  /** @type {string} */
  viewSourceElement.QDQOw = "_blank";
  const poweredBy = viewSourceElement;
  window.open(poweredBy.JfitT, poweredBy.QDQOw).focus();
});
/**
 * @return {?}
 */
function genTitle() {
  const malaquias = {};
  /**
   * @param {string} x
   * @param {string} b
   * @return {?}
   */
  malaquias.afIEb = function(x, b) {
    return x + b;
  };
  /** @type {string} */
  malaquias.KTLkS = "Re\u0430";
  /** @type {string} */
  malaquias.pXgLu = "\u200c\u200c&nbsp;OverIo\u0430d&nbsp;";
  /**
   * @param {number} n
   * @param {number} high
   * @return {?}
   */
  malaquias.YcaPg = function(n, high) {
    return n <= high;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  malaquias.vcdMJ = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  const selected = malaquias;
  let y = "";
  let i = "\u17b5\u200a";
  y = selected.afIEb(y, i);
  y = selected.afIEb(y, "i");
  y = selected.afIEb(y, i);
  y = selected.afIEb(y, selected.KTLkS);
  y = selected.afIEb(y, "dy");
  y = selected.afIEb(y, selected.pXgLu);
  for (let y = 0; selected.YcaPg(y, Math.floor(selected.vcdMJ(Math.random(), Math.floor(selected.vcdMJ(Math.random(), 12))))); y++) {
    y = selected.afIEb(y, i);
  }
  return y;
}
/**
 * @return {?}
 */
function genMinutes() {
  const jakeya = {};
  /**
   * @param {number} suppressDisabledCheck
   * @param {number} element
   * @return {?}
   */
  jakeya.hlEFG = function(suppressDisabledCheck, element) {
    return suppressDisabledCheck <= element;
  };
  /**
   * @param {number} mmCoreSecondsDay
   * @param {number} daysInterval
   * @return {?}
   */
  jakeya.IUJUG = function(mmCoreSecondsDay, daysInterval) {
    return mmCoreSecondsDay * daysInterval;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  jakeya.UpkWJ = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} n
   * @param {!Object} a
   * @return {?}
   */
  jakeya.cSjVx = function(n, a) {
    return n + a;
  };
  /**
   * @param {(Object|number)} n
   * @param {!Object} a
   * @return {?}
   */
  jakeya.QjVyY = function(n, a) {
    return n + a;
  };
  /** @type {string} */
  jakeya.hqcqI = "Mi\u200c\u200b\u2060n\u17b5\u17b5\u17b5\u17b5u";
  /**
   * @param {(Object|number)} line
   * @param {!Object} contextSize
   * @return {?}
   */
  jakeya.IPtcW = function(line, contextSize) {
    return line + contextSize;
  };
  /** @type {string} */
  jakeya.OZiuV = "t\u17b5\u17b5\u17b5e&nbsp;\u17b4Hac\u17b5\u17b5\u17b5k";
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  jakeya.lymtu = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  const self = jakeya;
  let line = "";
  let item = "\u061c";
  for (let element = 0; self.hlEFG(element, Math.floor(self.IUJUG(Math.random(), Math.floor(self.UpkWJ(Math.random(), 50))))); element++) {
    line = self.cSjVx(line, item);
  }
  line = self.QjVyY(line, self.hqcqI);
  line = self.IPtcW(line, self.OZiuV);
  for (let element = 0; self.hlEFG(element, Math.floor(self.lymtu(Math.random(), Math.floor(self.lymtu(Math.random(), 12))))); element++) {
    line = self.QjVyY(line, item);
  }
  return line;
}
/**
 * @return {?}
 */
function genSkipper() {
  const rusti = {};
  /**
   * @param {number} mid_OR_high
   * @param {number} high_OR_null
   * @return {?}
   */
  rusti.LKHfJ = function(mid_OR_high, high_OR_null) {
    return mid_OR_high <= high_OR_null;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  rusti.sxkXg = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  rusti.FZiiS = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} reason
   * @param {!Object} timeout
   * @return {?}
   */
  rusti.lVRFm = function(reason, timeout) {
    return reason + timeout;
  };
  /** @type {string} */
  rusti.FaPLa = "L\u00ad\u00ad\u00ad\u00ad\u00ades\u00ad\u00ad\u00ad\u00ad\u00ad\u00ads\u043e\u200bn&nbsp;s\u034fk\u00ad\u034f\u034f\u00ad\u034f\u00adi\u00ad\u034f\u034f\u034f\u034fpp\u00ad\u0435\u034f\u034f\u034fr";
  /**
   * @param {number} mid_OR_high
   * @param {number} high_OR_null
   * @return {?}
   */
  rusti.QlPTF = function(mid_OR_high, high_OR_null) {
    return mid_OR_high <= high_OR_null;
  };
  /**
   * @param {(Object|number)} reason
   * @param {!Object} timeout
   * @return {?}
   */
  rusti.PksDh = function(reason, timeout) {
    return reason + timeout;
  };
  const p = rusti;
  let value = "";
  let i = "\u200e\u061c\u200b";
  for (let col = 0; p.LKHfJ(col, Math.floor(p.sxkXg(Math.random(), Math.floor(p.FZiiS(Math.random(), 12))))); col++) {
    value = p.lVRFm(value, i);
  }
  value = p.lVRFm(value, p.FaPLa);
  for (let col = 0; p.QlPTF(col, Math.floor(p.FZiiS(Math.random(), Math.floor(p.sxkXg(Math.random(), 12))))); col++) {
    value = p.PksDh(value, i);
  }
  return value;
}
/**
 * @return {?}
 */
function genDiagnostic() {
  const fiori = {};
  /**
   * @param {number} mid_OR_high
   * @param {number} high_OR_null
   * @return {?}
   */
  fiori.FINNK = function(mid_OR_high, high_OR_null) {
    return mid_OR_high <= high_OR_null;
  };
  /**
   * @param {number} mmCoreSecondsDay
   * @param {number} daysInterval
   * @return {?}
   */
  fiori.ZEMPr = function(mmCoreSecondsDay, daysInterval) {
    return mmCoreSecondsDay * daysInterval;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  fiori.DLhMH = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} reason
   * @param {!Object} timeout
   * @return {?}
   */
  fiori.eiMaS = function(reason, timeout) {
    return reason + timeout;
  };
  /**
   * @param {(Object|number)} value
   * @param {!Object} increment
   * @return {?}
   */
  fiori.SeviU = function(value, increment) {
    return value + increment;
  };
  /** @type {string} */
  fiori.TcjTV = "Diagn\u200bos";
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  fiori.sRLdI = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} reason
   * @param {!Object} timeout
   * @return {?}
   */
  fiori.EVLgf = function(reason, timeout) {
    return reason + timeout;
  };
  /** @type {string} */
  fiori.XsfDT = "t\u200bi\u0441&nbsp;H\u200ba\u0441k";
  /**
   * @param {number} mid_OR_high
   * @param {number} high_OR_null
   * @return {?}
   */
  fiori.HjHKg = function(mid_OR_high, high_OR_null) {
    return mid_OR_high <= high_OR_null;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  fiori.zOzmJ = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} reason
   * @param {!Object} timeout
   * @return {?}
   */
  fiori.ysFPB = function(reason, timeout) {
    return reason + timeout;
  };
  const p = fiori;
  let value = "";
  let i = "\u061c\u200e\u200b\u200b\u200e";
  for (let col = 0; p.FINNK(col, Math.floor(p.ZEMPr(Math.random(), Math.floor(p.DLhMH(Math.random(), 12))))); col++) {
    value = p.eiMaS(value, i);
  }
  value = p.SeviU(value, p.TcjTV);
  for (let col = 0; p.FINNK(col, Math.floor(p.ZEMPr(Math.random(), Math.floor(p.sRLdI(Math.random(), 12))))); col++) {
    value = p.EVLgf(value, i);
  }
  value = p.eiMaS(value, p.XsfDT);
  for (let col = 0; p.HjHKg(col, Math.floor(p.DLhMH(Math.random(), Math.floor(p.zOzmJ(Math.random(), 12))))); col++) {
    value = p.ysFPB(value, i);
  }
  return value;
}
/**
 * @return {?}
 */
function genSkip() {
  const exports = {};
  /**
   * @param {number} mid_OR_high
   * @param {number} high_OR_null
   * @return {?}
   */
  exports.VHpWb = function(mid_OR_high, high_OR_null) {
    return mid_OR_high <= high_OR_null;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  exports.HhUTb = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  exports.bQgGO = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} reason
   * @param {!Object} timeout
   * @return {?}
   */
  exports.XVSVs = function(reason, timeout) {
    return reason + timeout;
  };
  /**
   * @param {(Object|number)} reason
   * @param {!Object} timeout
   * @return {?}
   */
  exports.kVRNt = function(reason, timeout) {
    return reason + timeout;
  };
  /** @type {string} */
  exports.SjwDB = "Skip";
  /**
   * @param {number} mid_OR_high
   * @param {number} high_OR_null
   * @return {?}
   */
  exports.mKjRw = function(mid_OR_high, high_OR_null) {
    return mid_OR_high <= high_OR_null;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  exports.HCrEj = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} reason
   * @param {!Object} timeout
   * @return {?}
   */
  exports.MXhAi = function(reason, timeout) {
    return reason + timeout;
  };
  /** @type {string} */
  exports.wYZnG = " c\u200burre";
  /**
   * @param {(Object|number)} reason
   * @param {!Object} timeout
   * @return {?}
   */
  exports.HKQzS = function(reason, timeout) {
    return reason + timeout;
  };
  /** @type {string} */
  exports.tLJRI = "nt&nbsp;lesson";
  const _this = exports;
  let value = "";
  let i = "\u061c\u200e\u200b\u200e\u200e";
  for (let col = 0; _this.VHpWb(col, Math.floor(_this.HhUTb(Math.random(), Math.floor(_this.bQgGO(Math.random(), 12))))); col++) {
    value = _this.XVSVs(value, i);
  }
  value = _this.kVRNt(value, _this.SjwDB);
  for (let col = 0; _this.mKjRw(col, Math.floor(_this.HhUTb(Math.random(), Math.floor(_this.HCrEj(Math.random(), 12))))); col++) {
    value = _this.MXhAi(value, i);
  }
  value = _this.MXhAi(value, _this.wYZnG);
  for (let col = 0; _this.mKjRw(col, Math.floor(_this.HhUTb(Math.random(), Math.floor(_this.bQgGO(Math.random(), 12))))); col++) {
    value = _this.HKQzS(value, i);
  }
  value = _this.HKQzS(value, _this.tLJRI);
  for (let col = 0; _this.VHpWb(col, Math.floor(_this.bQgGO(Math.random(), Math.floor(_this.HhUTb(Math.random(), 12))))); col++) {
    value = _this.kVRNt(value, i);
  }
  return value;
}
/**
 * @return {?}
 */
function genOtherMinutes() {
  const avilee = {};
  /**
   * @param {number} courseId
   * @param {number} canCreateDiscussions
   * @return {?}
   */
  avilee.tcKdS = function(courseId, canCreateDiscussions) {
    return courseId <= canCreateDiscussions;
  };
  /**
   * @param {number} mmCoreSecondsDay
   * @param {number} daysInterval
   * @return {?}
   */
  avilee.znrCb = function(mmCoreSecondsDay, daysInterval) {
    return mmCoreSecondsDay * daysInterval;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  avilee.DCQRv = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} datetimestamp
   * @param {!Object} canCreateDiscussions
   * @return {?}
   */
  avilee.pFoGY = function(datetimestamp, canCreateDiscussions) {
    return datetimestamp + canCreateDiscussions;
  };
  /** @type {string} */
  avilee.hnyda = "Fa\u200brm\u2002Min";
  /**
   * @param {number} mmCoreSecondsDay
   * @param {number} daysInterval
   * @return {?}
   */
  avilee.jHFNy = function(mmCoreSecondsDay, daysInterval) {
    return mmCoreSecondsDay * daysInterval;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  avilee.pmRNM = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} datetimestamp
   * @param {!Object} canCreateDiscussions
   * @return {?}
   */
  avilee.WOZaa = function(datetimestamp, canCreateDiscussions) {
    return datetimestamp + canCreateDiscussions;
  };
  /** @type {string} */
  avilee.NQjBi = "ut\u200bes";
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  avilee.CONPF = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} datetimestamp
   * @param {!Object} canCreateDiscussions
   * @return {?}
   */
  avilee.VlMFf = function(datetimestamp, canCreateDiscussions) {
    return datetimestamp + canCreateDiscussions;
  };
  const self = avilee;
  let date = "";
  let canCreateDiscussions = "\u200e\u061c\u200e\u061c\u200b\u200b\u200e";
  for (let courseId = 0; self.tcKdS(courseId, Math.floor(self.znrCb(Math.random(), Math.floor(self.DCQRv(Math.random(), 12))))); courseId++) {
    date = self.pFoGY(date, canCreateDiscussions);
  }
  date = self.pFoGY(date, self.hnyda);
  for (let courseId = 0; self.tcKdS(courseId, Math.floor(self.jHFNy(Math.random(), Math.floor(self.pmRNM(Math.random(), 12))))); courseId++) {
    date = self.WOZaa(date, canCreateDiscussions);
  }
  date = self.WOZaa(date, self.NQjBi);
  for (let courseId = 0; self.tcKdS(courseId, Math.floor(self.CONPF(Math.random(), 20))); courseId++) {
    date = self.VlMFf(date, canCreateDiscussions);
  }
  return date;
}
/**
 * @return {?}
 */
function genInfo() {
  const self = {
    CQdfh : "(((.+)+)+)+$",
    YSlPd : function(id, phone, state) {
      return id(phone, state);
    },
    xtERp : function(contextBuilder) {
      return contextBuilder();
    },
    TgWfj : function(courseId, canCreateDiscussions) {
      return courseId <= canCreateDiscussions;
    },
    siLFS : function(formatters, initialValue) {
      return formatters * initialValue;
    },
    FZFfN : function(value, prop) {
      return value + prop;
    },
    dSuPg : "P\u200bre",
    QVpeP : function(formatters, initialValue) {
      return formatters * initialValue;
    },
    unYmN : function(value, prop) {
      return value + prop;
    },
    BrWIs : function(value, increment) {
      return value + increment;
    },
    hRLuA : "ss c\u200btr",
    dvnfM : function(value, prop) {
      return value + prop;
    },
    qgJOA : function(value, increment) {
      return value + increment;
    },
    Zewmn : "l + z",
    ilpGq : function(mid_OR_high, high_OR_null) {
      return mid_OR_high <= high_OR_null;
    },
    ODjIC : function(formatters, initialValue) {
      return formatters * initialValue;
    },
    puDzo : function(value, increment) {
      return value + increment;
    },
    JHgvC : " t\u200bo hide",
    ltGap : function(mid_OR_high, high_OR_null) {
      return mid_OR_high <= high_OR_null;
    },
    idZno : function(formatters, initialValue) {
      return formatters * initialValue;
    },
    qlQON : " or sh\u200bow\u2002this",
    pfGTN : function(mid_OR_high, high_OR_null) {
      return mid_OR_high <= high_OR_null;
    },
    fiWPf : function(formatters, initialValue) {
      return formatters * initialValue;
    },
    poqcp : function(value, prop) {
      return value + prop;
    },
    zfFMm : function(value, prop) {
      return value + prop;
    },
    TQCPL : " panel",
    qETmG : function(mid_OR_high, high_OR_null) {
      return mid_OR_high <= high_OR_null;
    },
    AuMVr : function(formatters, initialValue) {
      return formatters * initialValue;
    },
    rKzau : function(value, prop) {
      return value + prop;
    },
    LpNmv : function(value, increment) {
      return value + increment;
    },
    ZSXUl : " at any\u2002time.",
    bPCAY : function(mid_OR_high, high_OR_null) {
      return mid_OR_high <= high_OR_null;
    }
  };
  const surveyId = function() {
    let y$$ = true;
    return function(s, fmt) {
      const voronoi = y$$ ? function() {
        if (fmt) {
          const code = fmt.apply(s, arguments);
          return fmt = null, code;
        }
      } : function() {
      };
      return y$$ = false, voronoi;
    };
  }();
  const b = self.YSlPd(surveyId, this, function() {
    return b.toString().search(self.CQdfh).toString().constructor(b).search(self.CQdfh);
  });
  self.xtERp(b);
  let password = "";
  let pass = "\u200e\u200b\u200b\u061c\u200e\u061c\u200e\u200b";
  for (let courseId = 0; self.TgWfj(courseId, Math.floor(self.siLFS(Math.random(), Math.floor(self.siLFS(Math.random(), 12))))); courseId++) {
    password = self.FZFfN(password, pass);
  }
  password = self.FZFfN(password, self.dSuPg);
  for (let courseId = 0; self.TgWfj(courseId, Math.floor(self.siLFS(Math.random(), Math.floor(self.QVpeP(Math.random(), 12))))); courseId++) {
    password = self.unYmN(password, pass);
  }
  password = self.BrWIs(password, self.hRLuA);
  for (let courseId = 0; self.TgWfj(courseId, Math.floor(self.siLFS(Math.random(), 20))); courseId++) {
    password = self.dvnfM(password, pass);
  }
  password = self.qgJOA(password, self.Zewmn);
  for (let $allPanels = 0; self.ilpGq($allPanels, Math.floor(self.ODjIC(Math.random(), 25))); $allPanels++) {
    password = self.dvnfM(password, pass);
  }
  password = self.puDzo(password, self.JHgvC);
  for (let $allPanels = 0; self.ltGap($allPanels, Math.floor(self.idZno(Math.random(), 25))); $allPanels++) {
    password = self.dvnfM(password, pass);
  }
  password = self.BrWIs(password, self.qlQON);
  for (let $allPanels = 0; self.pfGTN($allPanels, Math.floor(self.fiWPf(Math.random(), 20))); $allPanels++) {
    password = self.poqcp(password, pass);
  }
  password = self.zfFMm(password, self.TQCPL);
  for (let $allPanels = 0; self.qETmG($allPanels, Math.floor(self.AuMVr(Math.random(), 20))); $allPanels++) {
    password = self.rKzau(password, pass);
  }
  password = self.LpNmv(password, self.ZSXUl);
  for (let $allPanels = 0; self.bPCAY($allPanels, Math.floor(self.idZno(Math.random(), 20))); $allPanels++) {
    password = self.zfFMm(password, pass);
  }
  return password;
}
/**
 * @return {?}
 */
function genName() {
  const mikhiya = {};
  /**
   * @param {number} mid_OR_high
   * @param {number} high_OR_null
   * @return {?}
   */
  mikhiya.cpJbC = function(mid_OR_high, high_OR_null) {
    return mid_OR_high <= high_OR_null;
  };
  /**
   * @param {number} mmCoreSecondsDay
   * @param {number} daysInterval
   * @return {?}
   */
  mikhiya.XpVdr = function(mmCoreSecondsDay, daysInterval) {
    return mmCoreSecondsDay * daysInterval;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  mikhiya.DoIaJ = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} string
   * @param {!Object} replacement
   * @return {?}
   */
  mikhiya.DDSvh = function(string, replacement) {
    return string + replacement;
  };
  /** @type {string} */
  mikhiya.oqZgl = "\u0441u\u200bp";
  /**
   * @param {number} mid_OR_high
   * @param {number} high_OR_null
   * @return {?}
   */
  mikhiya.wbkno = function(mid_OR_high, high_OR_null) {
    return mid_OR_high <= high_OR_null;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  mikhiya.mmwmY = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} string
   * @param {!Object} replacement
   * @return {?}
   */
  mikhiya.ZvyaX = function(string, replacement) {
    return string + replacement;
  };
  /** @type {string} */
  mikhiya.zAMCL = "id\u0456tys (";
  /**
   * @param {number} mid_OR_high
   * @param {number} high_OR_null
   * @return {?}
   */
  mikhiya.mlhVv = function(mid_OR_high, high_OR_null) {
    return mid_OR_high <= high_OR_null;
  };
  /**
   * @param {number} mmCoreSecondsDay
   * @param {number} daysInterval
   * @return {?}
   */
  mikhiya.PCshW = function(mmCoreSecondsDay, daysInterval) {
    return mmCoreSecondsDay * daysInterval;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  mikhiya.ZErWJ = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} string
   * @param {!Object} replacement
   * @return {?}
   */
  mikhiya.TKuXx = function(string, replacement) {
    return string + replacement;
  };
  /** @type {string} */
  mikhiya.twqjW = "g\u0456thub)";
  const base = mikhiya;
  let value = "";
  let processUrl = "\u200b\u200b\u061c\u200b\u200b\u200b\u200b\u200b\u200b";
  for (let target_toc_item = 0; base.cpJbC(target_toc_item, Math.floor(base.XpVdr(Math.random(), Math.floor(base.DoIaJ(Math.random(), 16))))); target_toc_item++) {
    value = base.DDSvh(value, processUrl);
  }
  value = base.DDSvh(value, base.oqZgl);
  for (let target_toc_item = 0; base.wbkno(target_toc_item, Math.floor(base.DoIaJ(Math.random(), Math.floor(base.mmwmY(Math.random(), 16))))); target_toc_item++) {
    value = base.ZvyaX(value, processUrl);
  }
  value = base.ZvyaX(value, base.zAMCL);
  for (let target_toc_item = 0; base.mlhVv(target_toc_item, Math.floor(base.PCshW(Math.random(), Math.floor(base.ZErWJ(Math.random(), 16))))); target_toc_item++) {
    value = base.TKuXx(value, processUrl);
  }
  value = base.DDSvh(value, base.twqjW);
  for (let target_toc_item = 0; base.mlhVv(target_toc_item, Math.floor(base.DoIaJ(Math.random(), 20))); target_toc_item++) {
    value = base.DDSvh(value, processUrl);
  }
  return value;
}
/**
 * @return {?}
 */
function genName2() {
  const tarius = {};
  /** @type {string} */
  tarius.BYEwa = "join dis\u200f\u200f\u200e\u200ecord";
  /**
   * @param {number} mid_OR_high
   * @param {number} high_OR_null
   * @return {?}
   */
  tarius.fkMYw = function(mid_OR_high, high_OR_null) {
    return mid_OR_high <= high_OR_null;
  };
  /**
   * @param {number} formatters
   * @param {number} initialValue
   * @return {?}
   */
  tarius.RePCt = function(formatters, initialValue) {
    return formatters * initialValue;
  };
  /**
   * @param {(Object|number)} name
   * @param {!Object} timeout
   * @return {?}
   */
  tarius.NUpdY = function(name, timeout) {
    return name + timeout;
  };
  const $stateProvider = tarius;
  let chat = $stateProvider.BYEwa;
  let data = "\u200e\u200b\u200b\u061c\u200b\u200e\u200b\u200b\u200b\u200b\u200b\u200b";
  for (let page = 0; $stateProvider.fkMYw(page, Math.floor($stateProvider.RePCt(Math.random(), Math.floor($stateProvider.RePCt(Math.random(), 12))))); page++) {
    chat = $stateProvider.NUpdY(chat, data);
  }
  return chat;
}
/**
 * @param {!Element} div
 * @return {undefined}
 */
function dragElement(div) {
  /**
   * @param {!Object} event
   * @return {undefined}
   */
  function onmousedown(event) {
    const callbackVals = context.eXcJm.split("|");
    let callbackCount = 0;
    for (; true;) {
      switch(callbackVals[callbackCount++]) {
        case "0":
          event.preventDefault();
          continue;
        case "1":
          x = event.clientY;
          continue;
        case "2":
          event = event || window.event;
          continue;
        case "3":
          /** @type {function(!Object): undefined} */
          document.onmousemove = mouseMove;
          continue;
        case "4":
          /** @type {function(): undefined} */
          document.onmouseup = mouseUp;
          continue;
        case "5":
          left = event.clientX;
          continue;
      }
      break;
    }
  }
  /**
   * @param {!Object} event
   * @return {undefined}
   */
  function mouseMove(event) {
    const callbackVals = context.nNgdp.split("|");
    let callbackCount = 0;
    for (; true;) {
      switch(callbackVals[callbackCount++]) {
        case "0":
          div.style.top = context.aYWEe(context.wRZmJ(div.offsetTop, loc), "px");
          continue;
        case "1":
          x = event.clientY;
          continue;
        case "2":
          div.style.left = context.aYWEe(context.NTuAu(div.offsetLeft, expression), "px");
          continue;
        case "3":
          event = event || window.event;
          continue;
        case "4":
          left = event.clientX;
          continue;
        case "5":
          loc = context.wRZmJ(x, event.clientY);
          continue;
        case "6":
          expression = context.NTuAu(left, event.clientX);
          continue;
        case "7":
          event.preventDefault();
          continue;
      }
      break;
    }
  }
  /**
   * @return {undefined}
   */
  function mouseUp() {
    /** @type {null} */
    document.onmouseup = null;
    /** @type {null} */
    document.onmousemove = null;
  }
  const context = {
    EzZLb : function(Constructor, obj) {
      return Constructor(obj);
    },
    qlcAg : function(isSlidingUp, $cont) {
      return isSlidingUp + $cont;
    },
    YdIbQ : function(formatters, customFormatters) {
      return formatters + customFormatters;
    },
    HsWjM : "return (function() ",
    rDTLl : '{}.constructor("return this")( )',
    bQCZD : function(work) {
      return work();
    },
    lXuWE : "log",
    RInXq : "warn",
    pcRyp : "info",
    RIirl : "error",
    hqhOd : "exception",
    UHZGu : "table",
    vpQox : "trace",
    pnSAh : function(progressOld, progressNew) {
      return progressOld < progressNew;
    },
    eXcJm : "2|0|5|1|4|3",
    nNgdp : "3|7|6|5|4|1|0|2",
    aYWEe : function(size, unit) {
      return size + unit;
    },
    wRZmJ : function(connectNumber, concurency) {
      return connectNumber - concurency;
    },
    NTuAu : function(ast, expression) {
      return ast - expression;
    },
    pIHBg : function(test, name, featureObj) {
      return test(name, featureObj);
    },
    LIMFL : function(destroyed) {
      return destroyed();
    },
    fVZFr : "header",
    tJzPe : function(formatters, customFormatters) {
      return formatters + customFormatters;
    }
  };
  const obj = function() {
    let y$$ = true;
    return function(s, fmt) {
      const voronoi = y$$ ? function() {
        if (fmt) {
          const code = fmt.apply(s, arguments);
          return fmt = null, code;
        }
      } : function() {
      };
      return y$$ = false, voronoi;
    };
  }();
  const person = context.pIHBg(obj, this, function() {
    let o;
    try {
      const i = context.EzZLb(Function, context.qlcAg(context.YdIbQ(context.HsWjM, context.rDTLl), ");"));
      o = context.bQCZD(i);
    } catch (sheery) {
      /** @type {!Window} */
      o = window;
    }
    const args = o.console = o.console || {};
    const val = [context.lXuWE, context.RInXq, context.pcRyp, context.RIirl, context.hqhOd, context.UHZGu, context.vpQox];
    for (let index = 0; context.pnSAh(index, val.length); index++) {
      const err = obj.constructor.prototype.bind(obj);
      const i = val[index];
      const method = args[i] || err;
      err.__proto__ = obj.bind(obj);
      err.toString = method.toString.bind(method);
      args[i] = err;
    }
  });
  context.LIMFL(person);
  let expression = 0;
  let loc = 0;
  let left = 0;
  let x = 0;
  if (document.getElementById(context.aYWEe(div.id, context.fVZFr))) {
    /** @type {function(!Object): undefined} */
    document.getElementById(context.tJzPe(div.id, context.fVZFr)).onmousedown = onmousedown;
  } else {
    /** @type {function(!Object): undefined} */
    div.onmousedown = onmousedown;
  }
}
/**
 * @param {!Node} viewElement
 * @return {undefined}
 */
function skipLesson(viewElement) {
  /**
   * @return {undefined}
   */
  function _handleForm() {
    if (!html5Iframe.contentWindow.document.getElementById(options.QYPeU)) {
      options.OgdwB(alert, options.FLPkT);
      return;
    }
    let laugh = html5Iframe.src.split(options.sqVFb)[1].split(options.eGsDr)[0];
    let lowerVersion = html5Iframe.src.split(options.mIRjU)[1].split(options.sLsan)[0];
    html5Iframe.contentWindow.document.getElementById(options.QYPeU).click();
    html5Iframe.contentWindow.document.getElementsByClassName(options.zVPpT)[0].click();
    /** @type {boolean} */
    skippingLesson = true;
    viewElement.innerText = options.PTYMW;
    options.NeMQb(setTimeout, () => {
      const self = {
        MUEgm : function(aPanel_height, aInstance_id) {
          return options.hrcxU(aPanel_height, aInstance_id);
        },
        kmyZr : options.FxhLs,
        dTMza : options.hgfrJ,
        lHOMm : function(aPanel_height, aInstance_id) {
          return options.hrcxU(aPanel_height, aInstance_id);
        },
        zYoXW : function(instance, modifier) {
          return options.hFpjz(instance, modifier);
        },
        GVHgk : options.yidjg,
        xAvKk : function(instance, data) {
          return options.AQovm(instance, data);
        },
        Ldwgd : function(value, params) {
          return options.SUoTB(value, params);
        },
        PUtbG : function(name, base, position) {
          return options.BICzY(name, base, position);
        },
        djdYG : options.PEkgn,
        uuteD : options.QBChT,
        dthjq : options.ayItR,
        QWtyv : options.KBaHr,
        pVXDt : options.vnLPf,
        uXimM : options.oEZgl,
        FnIBS : options.GjYIZ,
        JThlu : options.uWkLv,
        EpCdd : options.XOlqD,
        szNAE : options.Fbmqu
      };
      options.BICzY(fetch, "https://login.i-ready.com/student/v2/web/appstate/" + lowerVersion + "?bucket=short_term_unsecured&datatype=json", {
        headers : {
          accept : options.PEkgn,
          "accept-language" : options.QBChT,
          "sec-ch-ua-mobile" : "?0",
          "sec-ch-ua-platform" : options.IWuUa,
          "sec-fetch-dest" : options.KBaHr,
          "sec-fetch-mode" : options.vnLPf,
          "sec-fetch-site" : options.oEZgl
        },
        referrer : options.GjYIZ,
        referrerPolicy : options.uWkLv,
        body : null,
        method : options.bxjur,
        mode : options.vnLPf,
        credentials : options.Fbmqu
      }).then((achiev) => {
        return achiev.text();
      }).then((strBody) => {
        let presenter = JSON.parse(strBody);
        let knownInsta = self.MUEgm(parseInt, Object.keys(presenter.resultsStore.results).length);
        if (self.zYoXW(laugh, self.GVHgk)) {
          let levella = false;
          if (self.xAvKk(knownInsta, 5)) {
            if (self.zYoXW(Math.round(self.Ldwgd(Math.random(), 1.4)), 1)) {
              /** @type {boolean} */
              levella = true;
            }
          }
          let pingPongTimeout;
          let exportFuncs = presenter.navigationStore.stepState;
          for (let ctrlname in exportFuncs) {
            if (exportFuncs.hasOwnProperty(ctrlname)) {
              pingPongTimeout = ctrlname;
            }
          }
          let calub = presenter.navigationStore.currentStep;
          let willer = presenter.navigationStore.highestReachedStepId;
          let GroupRightsDefer = strBody.replaceAll('"complete":false', '"complete":true');
          let NEEDED_FILE_PATH = GroupRightsDefer.replaceAll('"finished":false', '"finished":true');
          let defendantMessage = NEEDED_FILE_PATH.replaceAll('"visited":false', '"visited":true');
          let observersMessage = defendantMessage.replaceAll('"raw":1', '"raw":0');
          let attackerMessage = observersMessage.replaceAll('"max":1', '"max":0');
          let boneGroupName = attackerMessage.replaceAll('"currentStep":"' + calub + '"', '"currentStep":"' + pingPongTimeout + '"');
          boneGroupName = boneGroupName.replaceAll('"raw":0', '"raw":1');
          boneGroupName = boneGroupName.replaceAll('"max":0', '"max":1');
          let currReplacement = boneGroupName.replaceAll('"highestReachedStepId":"' + willer + '"', '"highestReachedStepId":"' + pingPongTimeout + '"');
          let emotes = currReplacement.replaceAll("\\", "@@@@@@@@");
          let smsText = emotes.replaceAll('"', '\\"');
          let olus = smsText.replaceAll("@@@@@@@@", "\\\\");
          self.PUtbG(fetch, "https://login.i-ready.com/student/v2/web/lesson_component/" + lowerVersion + "/markprogress/" + lowerVersion, {
            headers : {
              accept : self.djdYG,
              "accept-language" : self.uuteD,
              "content-type" : self.dthjq,
              "sec-ch-ua-mobile" : "?0",
              "sec-fetch-dest" : self.QWtyv,
              "sec-fetch-mode" : self.pVXDt,
              "sec-fetch-site" : self.uXimM
            },
            referrer : self.FnIBS,
            referrerPolicy : self.JThlu,
            body : '{"value":"' + olus + '","bucket":"short_term_unsecured","datatype":"json"}',
            method : self.EpCdd,
            mode : self.pVXDt,
            credentials : self.szNAE
          });
          self.PUtbG(setTimeout, function() {
            self.MUEgm(alert, self.kmyZr);
            /** @type {boolean} */
            skippingLesson = false;
            viewElement.innerText = self.dTMza;
          }, 250);
        } else {
          let pingPongTimeout;
          let exportFuncs = presenter.navigationStore.stepState;
          for (let ctrlname in exportFuncs) {
            if (exportFuncs.hasOwnProperty(ctrlname)) {
              pingPongTimeout = ctrlname;
            }
          }
          let kamelah = presenter.navigationStore.currentStep;
          let poyraz = presenter.navigationStore.highestReachedStepId;
          let GroupRightsDefer = strBody.replaceAll('"complete":false', '"complete":true');
          let NEEDED_FILE_PATH = GroupRightsDefer.replaceAll('"finished":false', '"finished":true');
          let boneGroupName = NEEDED_FILE_PATH.replaceAll('"visited":false', '"visited":true');
          let attackerMessage = boneGroupName.replaceAll('"currentStep":"' + kamelah + '"', '"currentStep":"' + pingPongTimeout + '"');
          let currReplacement = attackerMessage.replaceAll('"highestReachedStepId":"' + poyraz + '"', '"highestReachedStepId":"' + pingPongTimeout + '"');
          let observersMessage = currReplacement.replaceAll('"raw":0,"max":0', '"raw":1,"max":1');
          let defendantMessage = observersMessage.replaceAll("\\", "@@@@@@@@");
          let emotes = defendantMessage.replaceAll('"', '\\"');
          let dallan = emotes.replaceAll("@@@@@@@@", "\\\\");
          self.PUtbG(fetch, "https://login.i-ready.com/student/v2/web/lesson_component/" + lowerVersion + "/markprogress/" + lowerVersion, {
            headers : {
              accept : self.djdYG,
              "accept-language" : self.uuteD,
              "content-type" : self.dthjq,
              "sec-ch-ua-mobile" : "?0",
              "sec-fetch-dest" : self.QWtyv,
              "sec-fetch-mode" : self.pVXDt,
              "sec-fetch-site" : self.uXimM
            },
            referrer : self.FnIBS,
            referrerPolicy : self.JThlu,
            body : '{"value":"' + dallan + '","bucket":"short_term_unsecured","datatype":"json"}',
            method : self.EpCdd,
            mode : self.pVXDt,
            credentials : self.szNAE
          });
          self.PUtbG(setTimeout, function() {
            self.lHOMm(alert, self.kmyZr);
            /** @type {boolean} */
            skippingLesson = false;
            viewElement.innerText = self.dTMza;
          }, 250);
        }
      });
    }, 1e3);
  }
  /**
   * @return {undefined}
   */
  function render() {
    const img = {
      azrPc : function(val, data) {
        return options.fFgUP(val, data);
      }
    };
    let currentDataItemIdentifyingStr = html5Iframe.src.split("/")[options.zNNMy(html5Iframe.src.split("/").length, 1)];
    let inventoryKeys = html5Iframe.src.split(options.mIRjU)[1].split(options.sLsan)[0];
    let yatziry = currentDataItemIdentifyingStr.split("_")[options.zNNMy(currentDataItemIdentifyingStr.split("_").length, 1)];
    let _ddoc = inventoryKeys.includes(options.HmzAr) ? options.HmzAr : options.QIfMx;
    let daviaun = html5Iframe.src.split(options.sqVFb)[1].split(options.eGsDr)[0];
    let newNodeLists = [];
    let dynviews = [];
    let success;
    const headers = {};
    headers.accept = options.qLAER;
    headers["accept-language"] = options.QBChT;
    headers["sec-fetch-dest"] = options.KBaHr;
    headers["sec-fetch-mode"] = options.vnLPf;
    headers["sec-fetch-site"] = options.oEZgl;
    /** @type {string} */
    headers["sec-gpc"] = "1";
    const params = {};
    params.headers = headers;
    params.referrer = options.GjYIZ;
    params.referrerPolicy = options.uWkLv;
    /** @type {null} */
    params.body = null;
    params.method = options.bxjur;
    params.mode = options.vnLPf;
    params.credentials = options.Fbmqu;
    html5Iframe.contentWindow.fetch("https://cdn.i-ready.com/instruction/phoenix-content/" + _ddoc + "/" + currentDataItemIdentifyingStr + "/lessonStructure.json", params).then((rawResp) => {
      return rawResp.json();
    }).then(function(canCreateDiscussions) {
      console.log();
      success = canCreateDiscussions.lessonStructureObj.slideData.length;
      /** @type {!Array} */
      newNodeLists = [];
      /** @type {!Array} */
      dynviews = [];
      for (let j = 0; img.azrPc(j, success); j++) {
        newNodeLists.push(true);
        dynviews.push(1);
      }
      /** @type {string} */
      document.cookie = "lessonId=" + currentDataItemIdentifyingStr + ";";
      /** @type {string} */
      document.cookie = "lessonType=" + yatziry + ";";
      /** @type {string} */
      document.cookie = "subject=" + _ddoc + ";";
      /** @type {string} */
      document.cookie = "csid=" + inventoryKeys + ";";
      /** @type {string} */
      document.cookie = "slidesCompletedArr=" + newNodeLists + ";";
      /** @type {string} */
      document.cookie = "scoreArr=" + dynviews + ";";
      /** @type {string} */
      document.cookie = "slideCount=" + success + ";";
      /** @type {string} */
      document.cookie = "type=" + daviaun + ";";
    });
    options.mElgO(alert, options.vZqyu);
    options.MGjHa(alert, options.AoqFp);
    /** @type {boolean} */
    skippingLesson2 = true;
    /** @type {boolean} */
    skippingLesson = true;
    viewElement.innerText = options.PTYMW;
  }
  const options = {
    hrcxU : function(aPanel_height, aInstance_id) {
      return aPanel_height(aInstance_id);
    },
    FxhLs : "Enter the lesson and you should see it was skipped.",
    hgfrJ : "Skip current lesson",
    hFpjz : function(factory, type) {
      return factory == type;
    },
    yidjg : "QUIZ",
    AQovm : function(num, val) {
      return num > val;
    },
    SUoTB : function(_context, authorizerId) {
      return _context * authorizerId;
    },
    BICzY : function(callback, offset, width) {
      return callback(offset, width);
    },
    PEkgn : "**",
    mElgO : function(factory, type) {
      return factory(type);
    },
    vZqyu : "Disclaimer: Skipping lessons without farming minutes first may get you in trouble as teachers can see how much time you spent on a lesson. (if you already got minutes you can ignore this)",
    MGjHa : function(factory, type) {
      return factory(type);
    },
    AoqFp : "To skip this lesson, close the lesson and click the skip button again.",
    AEGex : function(renderFunc) {
      return renderFunc();
    },
    AFKnT : "Using method 1",
    uadWs : function(match, name) {
      return match == name;
    },
    CMTQO : "Your current lesson schema is not supported. If you'd like support, create an issue on the github.",
    nqKMb : function(type, group) {
      return type != group;
    },
    svPNq : function(action, callBack, params) {
      return action(callBack, params);
    },
    dquyj : "Using method 2",
    OLUAl : "continue-button-button",
    bnoCW : "btn-footer-MY_PROGRESS-button",
    woqvO : "btn-footer-TO_DO-button",
    jvWWa : "html5Iframe",
    JppXZ : function(factory, type) {
      return factory(type);
    },
    ipUix : "You do not have a lesson currently open. You must open a lesson to skip it.",
    XSNRp : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    jioXl : "lessonId",
    kWfAZ : "lessonType",
    FliAi : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    VnDkF : "csid",
    mHOFW : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    bgmXY : "slidesCompletedArr",
    yjuZw : "scoreArr",
    lcJxf : function(origParseInt, string, radix) {
      return origParseInt(string, radix);
    },
    GXLDk : "slideCount",
    KalLk : function(factory, type) {
      return factory(type);
    },
    iCbyP : "type",
    dTGGF : function(action, callBack, params) {
      return action(callBack, params);
    },
    iIpEE : function(withError, list) {
      return withError - list;
    },
    ZtbUa : function(factory, type) {
      return factory(type);
    },
    AqADg : "Open the lesson and you should see it was skipped. You may need to answer the final question (anything you put down will be marked as correct)",
    Pfest : function(fn, callback, type) {
      return fn(callback, type);
    },
    hichD : function(geom0, geom1) {
      return geom0 && geom1;
    },
    HJeZn : function(factory, type) {
      return factory(type);
    },
    UbeRQ : "You're already skipping a lesson."
  };
  if (!window[options.jvWWa] && !skippingLesson) {
    options.JppXZ(alert, options.ipUix);
  } else {
    if (window[options.jvWWa] && !skippingLesson && !skippingLesson2) {
      let picKey = html5Iframe.src.split("/")[options.zNNMy(html5Iframe.src.split("/").length, 1)];
      let inventoryKeys = html5Iframe.src.split(options.mIRjU)[1].split(options.sLsan)[0];
      let _ddoc = inventoryKeys.includes(options.HmzAr) ? options.HmzAr : options.QIfMx;
      const headers = {};
      headers.accept = options.qLAER;
      headers["accept-language"] = options.QBChT;
      headers["sec-fetch-dest"] = options.KBaHr;
      headers["sec-fetch-mode"] = options.vnLPf;
      headers["sec-fetch-site"] = options.oEZgl;
      /** @type {string} */
      headers["sec-gpc"] = "1";
      const params = {};
      params.headers = headers;
      params.referrer = options.GjYIZ;
      params.referrerPolicy = options.uWkLv;
      /** @type {null} */
      params.body = null;
      params.method = options.bxjur;
      params.mode = options.vnLPf;
      params.credentials = options.Fbmqu;
      html5Iframe.contentWindow.fetch("https://cdn.i-ready.com/instruction/phoenix-content/" + _ddoc + "/" + picKey + "/lessonStructure.json", params).then(function(event) {
        if (options.nqKMb(event.status, 200)) {
          options.svPNq(fetch, "https://login.i-ready.com/student/v2/web/appstate/" + inventoryKeys + "?bucket=short_term_unsecured&datatype=json", {
            headers : {
              accept : options.qLAER,
              "accept-language" : options.QBChT,
              "sec-fetch-dest" : options.KBaHr,
              "sec-fetch-mode" : options.vnLPf,
              "sec-fetch-site" : options.oEZgl,
              "sec-gpc" : "1"
            },
            referrer : options.GjYIZ,
            referrerPolicy : options.uWkLv,
            body : null,
            method : options.bxjur,
            mode : options.vnLPf,
            credentials : options.Fbmqu
          }).then(function(event) {
            if (options.hFpjz(event.status, 200)) {
              options.AEGex(_handleForm);
              console.log(options.AFKnT);
            } else {
              if (options.uadWs(event.status, 404)) {
                options.MGjHa(alert, options.CMTQO);
              }
            }
          });
        } else {
          options.AEGex(render);
          console.log(options.dquyj);
        }
      });
    } else {
      if (!window[options.jvWWa] && skippingLesson2) {
        let movita = options.XSNRp(getCookie, options.jioXl);
        let symmone = options.OgdwB(getCookie, options.kWfAZ);
        let showingFrom = options.FliAi(getCookie, options.VnDkF);
        let trista = options.mHOFW(getCookie, options.bgmXY);
        let larone = options.XSNRp(getCookie, options.yjuZw);
        let error = options.lcJxf(parseInt, options.FliAi(getCookie, options.GXLDk), 10);
        let parsedData = options.KalLk(getCookie, options.iCbyP);
        options.dTGGF(fetch, "https://login.i-ready.com/student/v2/web/lesson_component/" + showingFrom + "/markprogress/" + showingFrom, {
          headers : {
            accept : options.PEkgn,
            "accept-language" : options.QBChT,
            "content-type" : options.ayItR,
            "sec-fetch-dest" : options.KBaHr,
            "sec-fetch-mode" : options.vnLPf,
            "sec-fetch-site" : options.oEZgl,
            "sec-gpc" : "1"
          },
          referrer : options.GjYIZ,
          referrerPolicy : options.uWkLv,
          body : '{"value":"{\\"fi' + "nalStageAr" + 'r\\":[' + '],\\"c' + "urSlideInt" + '\\":' + options.iIpEE(error, 1) + (',\\"sl' + "ideCompletedArr" + '\\":[') + trista + ('],\\"a' + "llScoreDataArr\\" + '":[') + larone + ('],\\"s' + "tateStoreDataOb" + 'j\\":{}}","' + "bucke" + 't":"s' + "hort_term_unsec" + 'ured"' + ',"dat' + "atype" + '":"json"}'),
          method : options.XOlqD,
          mode : options.vnLPf,
          credentials : options.Fbmqu
        });
        if (options.uadWs(parsedData, options.yidjg)) {
          options.ZtbUa(alert, options.AqADg);
          let incomingLayer = options.Pfest(setInterval, function() {
            if (document.getElementById(options.OLUAl) || document.getElementById(options.bnoCW) || document.getElementById(options.woqvO)) {
              options.hrcxU(clearInterval, incomingLayer);
              /** @type {boolean} */
              skippingLesson = false;
              /** @type {boolean} */
              skippingLesson2 = false;
              viewElement.innerText = options.hgfrJ;
            }
          }, 500);
        } else {
          options.KalLk(alert, options.AqADg);
          /** @type {boolean} */
          skippingLesson2 = false;
          /** @type {boolean} */
          skippingLesson = false;
          viewElement.innerText = options.hgfrJ;
        }
      } else {
        if (options.hichD(skippingLesson, !skippingLesson2)) {
          options.HJeZn(alert, options.UbeRQ);
        } else {
          if (skippingLesson2 && window[options.jvWWa]) {
            options.KalLk(alert, options.AoqFp);
          }
        }
      }
    }
  }
}
/**
 * @param {!Node} LoginScreenPlugin
 * @return {undefined}
 */
function farmMinutes(LoginScreenPlugin) {
  const options = {
    Pndst : function(factory, type) {
      return factory(type);
    },
    WSSAF : function(mid_OR_high, high_OR_null) {
      return mid_OR_high <= high_OR_null;
    },
    DFoHH : "0|1|2|3|4",
    EMpMB : function(type, key, oldVal) {
      return type(key, oldVal);
    },
    qzPdD : "application/json, text/plain, */*",
    ETgem : "en-US,en;q=0.9",
    bJDEd : "empty",
    fkQYD : "cors",
    DzHEz : "same-origin",
    pmcYk : "https://login.i-ready.com/student/dashboard/home",
    JNUMk : "strict-origin-when-cross-origin",
    shQQK : "GET",
    wZGVP : "include",
    eArYK : function(action, callBack, params) {
      return action(callBack, params);
    },
    UphNZ : function(value, list) {
      return value == list;
    },
    GaYAl : "btn-footer-MY_PROGRESS",
    CGZvB : "btn-footer-TO_DO",
    CDIuS : "3|1|5|4|0|6|2",
    bkJlY : function(name, filename, t) {
      return name(filename, t);
    },
    FvUNo : "Farm minutes",
    namKI : function(func, layer) {
      return func(layer);
    },
    QnGDh : "7|4|0|1|6|2|3|5",
    VvbOF : function(f, a) {
      return f(a);
    },
    avnpf : function(type, key, oldVal) {
      return type(key, oldVal);
    },
    pCkzY : function(factory, type) {
      return factory(type);
    },
    EOEbm : "Stopped",
    iryZm : "csid",
    lroww : "html5Iframe",
    BkHmU : "?csid=",
    mUrai : "&type",
    RdopB : "How many minutes would you like to farm?",
    VmUPI : function(factory, type) {
      return factory(type);
    },
    ElkXE : "Neccessary data to farm minutes have now been collected. To begin farming minutes, go to the iReady menu by closing this lesson/quiz. Then, press this button again.",
    ZCsoT : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    Oxnqq : function(factory, type) {
      return factory(type);
    },
    nwCFJ : "You do not have a lesson currently open. You must open a lesson to begin the proccess.",
    cJxhv : "minutes",
    JPvNy : function(action, callBack, params) {
      return action(callBack, params);
    },
    vDwcJ : function(factory, type) {
      return factory(type);
    },
    DcxQG : 'The minute farming process has now begun. Do not close this page. Do not turn off your computer. After you press "ok," every minute that passes will be added to your account. When you want to stop the timer, press the button labeled "Stop farming minutes". Press "ok" to begin.'
  };
  if (minuteFarming) {
    const callbackVals = options.QnGDh.split("|");
    let callbackCount = 0;
    for (; true;) {
      switch(callbackVals[callbackCount++]) {
        case "0":
          /** @type {string} */
          document.cookie = 'csid=; expires=Thu, 18 Dec 1970 12:00:00 UTC"';
          continue;
        case "1":
          /** @type {string} */
          document.cookie = 'minutes=; expires=Thu, 18 Dec 1970 12:00:00 UTC"';
          continue;
        case "2":
          /** @type {boolean} */
          minuteFarming = false;
          continue;
        case "3":
          options.VvbOF(clearInterval, lolol);
          continue;
        case "4":
          options.avnpf(fetch, "https://login.i-ready.com/student/v1/web/lesson_component/" + csid + "?action=pause", {
            headers : {
              accept : options.qzPdD,
              "accept-language" : options.ETgem,
              "sec-fetch-dest" : options.bJDEd,
              "sec-fetch-mode" : options.fkQYD,
              "sec-fetch-site" : options.DzHEz
            },
            referrer : options.pmcYk,
            referrerPolicy : options.JNUMk,
            body : null,
            method : options.shQQK,
            mode : options.fkQYD,
            credentials : options.wZGVP
          });
          continue;
        case "5":
          options.pCkzY(alert, options.EOEbm);
          continue;
        case "6":
          LoginScreenPlugin.innerText = options.FvUNo;
          continue;
        case "7":
          csid = options.pCkzY(getCookie, options.iryZm);
          continue;
      }
      break;
    }
  }
  if (!!window[options.lroww]) {
    let valayah = html5Iframe.src.split(options.BkHmU)[1].split(options.mUrai)[0];
    let tehilla = options.bkJlY(prompt, options.RdopB, 30);
    /** @type {string} */
    document.cookie = "csid=" + valayah + '; expires=Thu, 18 Dec 2999 12:00:00 UTC"';
    /** @type {string} */
    document.cookie = "minutes=" + tehilla + '; expires=Thu, 18 Dec 2999 12:00:00 UTC"';
    options.VmUPI(alert, options.ElkXE);
  } else {
    if (!options.ZCsoT(getCookie, options.iryZm)) {
      options.Oxnqq(alert, options.nwCFJ);
    } else {
      /**
       * @return {undefined}
       */
      var handleRequestResponse = function() {
        if (options.WSSAF(error, minutestofarm)) {
          const callbackVals = options.DFoHH.split("|");
          let callbackCount = 0;
          for (; true;) {
            switch(callbackVals[callbackCount++]) {
              case "0":
                options.EMpMB(fetch, "https://login.i-ready.com/student/v1/web/lesson_component/" + csid + "?action=pause", {
                  headers : {
                    accept : options.qzPdD,
                    "accept-language" : options.ETgem,
                    "sec-fetch-dest" : options.bJDEd,
                    "sec-fetch-mode" : options.fkQYD,
                    "sec-fetch-site" : options.DzHEz
                  },
                  referrer : options.pmcYk,
                  referrerPolicy : options.JNUMk,
                  body : null,
                  method : options.shQQK,
                  mode : options.fkQYD,
                  credentials : options.wZGVP
                });
                continue;
              case "1":
                options.eArYK(fetch, "https://login.i-ready.com/student/v1/web/lesson_component/" + csid + "?action=resume", {
                  headers : {
                    accept : options.qzPdD,
                    "accept-language" : options.ETgem,
                    "sec-fetch-dest" : options.bJDEd,
                    "sec-fetch-mode" : options.fkQYD,
                    "sec-fetch-site" : options.DzHEz
                  },
                  referrer : options.pmcYk,
                  referrerPolicy : options.JNUMk,
                  body : null,
                  method : options.shQQK,
                  mode : options.fkQYD,
                  credentials : options.wZGVP
                });
                continue;
              case "2":
                error++;
                continue;
              case "3":
                if (options.UphNZ(error, 1)) {
                  /** @type {string} */
                  LoginScreenPlugin.innerText = "Gained " + error + " minute | Click to stop";
                } else {
                  /** @type {string} */
                  LoginScreenPlugin.innerText = "Gained " + error + " minutes | Click to stop";
                }
                continue;
              case "4":
                if (document.getElementById(options.GaYAl) && document.getElementById(options.CGZvB)) {
                  document.getElementById(options.GaYAl).click();
                  document.getElementById(options.CGZvB).click();
                }
                continue;
            }
            break;
          }
        } else {
          const callbackVals = options.CDIuS.split("|");
          let callbackCount = 0;
          for (; true;) {
            switch(callbackVals[callbackCount++]) {
              case "0":
                /** @type {boolean} */
                minuteFarming = false;
                continue;
              case "1":
                /** @type {string} */
                document.cookie = 'csid=; expires=Thu, 18 Dec 1970 12:00:00 UTC"';
                continue;
              case "2":
                options.bkJlY(setTimeout, function() {
                  options.Pndst(alert, "Successfully gained " + minutestofarm + " minutes");
                }, 500);
                continue;
              case "3":
                options.EMpMB(fetch, "https://login.i-ready.com/student/v1/web/lesson_component/" + csid + "?action=pause", {
                  headers : {
                    accept : options.qzPdD,
                    "accept-language" : options.ETgem,
                    "sec-fetch-dest" : options.bJDEd,
                    "sec-fetch-mode" : options.fkQYD,
                    "sec-fetch-site" : options.DzHEz
                  },
                  referrer : options.pmcYk,
                  referrerPolicy : options.JNUMk,
                  body : null,
                  method : options.shQQK,
                  mode : options.fkQYD,
                  credentials : options.wZGVP
                });
                continue;
              case "4":
                LoginScreenPlugin.innerText = options.FvUNo;
                continue;
              case "5":
                /** @type {string} */
                document.cookie = 'minutes=; expires=Thu, 18 Dec 1970 12:00:00 UTC"';
                continue;
              case "6":
                options.namKI(clearInterval, incomingLayer);
                continue;
            }
            break;
          }
        }
      };
      csid = options.Pndst(getCookie, options.iryZm);
      minutestofarm = options.Pndst(getCookie, options.cJxhv);
      let error = 0;
      let incomingLayer = options.avnpf(setInterval, handleRequestResponse, 6e4);
      /** @type {boolean} */
      minuteFarming = true;
      /** @type {string} */
      LoginScreenPlugin.innerText = "Gained " + error + " minutes | Click to stop";
      options.JPvNy(fetch, "https://login.i-ready.com/student/v1/web/lesson_component/" + csid + "?action=resume", {
        headers : {
          accept : options.qzPdD,
          "accept-language" : options.ETgem,
          "sec-fetch-dest" : options.bJDEd,
          "sec-fetch-mode" : options.fkQYD,
          "sec-fetch-site" : options.DzHEz
        },
        referrer : options.pmcYk,
        referrerPolicy : options.JNUMk,
        body : null,
        method : options.shQQK,
        mode : options.fkQYD,
        credentials : options.wZGVP
      });
      options.vDwcJ(alert, options.DcxQG);
    }
  }
}
/**
 * @param {?} canCreateDiscussions
 * @return {undefined}
 */
function diagnosticHack(canCreateDiscussions) {
  const task = {
    ZpQwC : function(text, contextClosing) {
      return text == contextClosing;
    },
    wuGsg : "undefined",
    QhnvY : function(factory, type) {
      return factory(type);
    },
    kddut : "Diagnostic not detected. Open the diagnostic first to use the hack.",
    iHejM : function(factory, type) {
      return factory(type);
    },
    Qnbii : "the diagnostic hack doesnt actually work rn rofl"
  };
  if (task.ZpQwC(typeof diagnosticIFrame, task.wuGsg)) {
    task.QhnvY(alert, task.kddut);
  } else {
    task.iHejM(alert, task.Qnbii);
  }
}
/**
 * @param {string} detail
 * @return {?}
 */
function getCookie(detail) {
  const req = {
    AhdQe : function(detailsA, detailsB) {
      return detailsA + detailsB;
    },
    PWDnV : function(func, encoder) {
      return func(encoder);
    },
    Iszzr : function(progressOld, progressNew) {
      return progressOld < progressNew;
    },
    DtScR : function(license, buildingApp) {
      return license == buildingApp;
    }
  };
  let name = req.AhdQe(detail, "=");
  let componentsStr = req.PWDnV(decodeURIComponent, document.cookie);
  let bucket = componentsStr.split(";");
  for (let i = 0; req.Iszzr(i, bucket.length); i++) {
    let chips = bucket[i];
    for (; req.DtScR(chips.charAt(0), " ");) {
      chips = chips.substring(1);
    }
    if (req.DtScR(chips.indexOf(name), 0)) {
      return chips.substring(name.length, chips.length);
    }
  }
  return "";
}
;
